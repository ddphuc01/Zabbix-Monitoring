# Ansible Inventory - Synced from Zabbix
# Auto-generated by sync_inventory.py
# Last updated: 2026-01-05 - Added localhost for testing

all:
  children:
    production:
      hosts:
        # Add your production hosts here
        # Example:
        # web-server-01:
        #   ansible_host: 192.168.1.10
        #   ansible_user: ubuntu
        #   zabbix_hostid: "10001"
    
    staging:
      hosts:
        # Add staging hosts here
    
    monitoring:
      hosts:
        zabbix-server:
          ansible_host: localhost
          ansible_connection: local
          zabbix_hostid: "10000"
    
    test:
      hosts:
        localhost:
          ansible_host: 127.0.0.1
          ansible_user: phuc
          ansible_ssh_private_key_file: /root/.ssh/ansible_key
          zabbix_hostid: "test-001"
    
    windows:
      vars:
        ansible_connection: winrm
        ansible_winrm_transport: basic
        ansible_winrm_server_cert_validation: ignore
        ansible_port: 5985
      hosts:
        win-pc-129:
          ansible_host: 192.168.1.129
          ansible_user: rog
          ansible_password: "123"
          zabbix_hostid: "win-001"
