# PostgreSQL Database Configuration
# This file follows official Zabbix Docker pattern
# See: https://github.com/zabbix/zabbix-docker

# IMPORTANT: Copy this file to .env_db_pgsql and fill in your credentials
# cp env_vars/.env_db_pgsql.example env_vars/.env_db_pgsql

# Database connection
POSTGRES_USER=zabbix
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD_HERE
POSTGRES_DB=zabbix

# Performance tuning (optional, defaults work for most cases)
# For servers with 4GB+ RAM, increase these:
POSTGRES_SHARED_BUFFERS=256MB
POSTGRES_EFFECTIVE_CACHE_SIZE=512MB
POSTGRES_MAINTENANCE_WORK_MEM=128MB
POSTGRES_WAL_BUFFERS=8MB
POSTGRES_MAX_CONNECTIONS=100

# For servers with 8GB+ RAM, use:
# POSTGRES_SHARED_BUFFERS=512MB
# POSTGRES_EFFECTIVE_CACHE_SIZE=1GB
# POSTGRES_MAINTENANCE_WORK_MEM=256MB
